import 'package:flutter/material.dart';

class LogInTab extends StatelessWidget {
  const LogInTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Log in tab'),
    );
  }
}
